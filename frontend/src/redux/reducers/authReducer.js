
const initialState = {
  // see if it is authenticated
  observable: true,
}

export const authReducer = (state = initialState, action) => {
  switch (action.type) {
  
    default:
      return state;
  }
}